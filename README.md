### swar-reloaded (alpha)
remake of my old game, which was itself a clone of classic game Spacewar!

`./make.sh build` - build

`./make.sh run` - run

`./make.sh jar` - create jar

controls:

arrows - move

ctrl - fire

1-4 - switch team

q-r - add bots